const { Client } = require('pg'); const { PrismaClient } = require('@prisma/client'); async function migrate() { const pgClient = new Client({ connectionString: 'postgres://postgres:postgres@localhost:51214/template1' }); await pgClient.connect(); const res = await pgClient.query('SELECT table_name FROM information_schema.tables WHERE table_schema = ''public'''); console.log(res.rows); await pgClient.end(); } migrate();
